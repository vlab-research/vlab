import { ConfigBase, FieldState } from '../../../../types/form';
import { Event } from '../../../../helpers/updateLocalState';

const recruitment = (config: ConfigBase, state: FieldState, event: Event) => {
  // TODO create a type for State
  // if (event.field === 'type') {
  //   const addedConf = config.selectorOptions[event.value];
  //   return { ...state, ...createInitialState(addedConf) };
  // }
  // return state;
};

export default recruitment;
