{
  "$comment": "Generated from adopt/adopt/study_conf.py and adopt/adopt/study_conf_strict.py by adopt/adopt/schema_export.py. Do not edit by hand; run `make schemas`. Body of POST /{org_id}/studies/{slug}/confs/audiences (a JSON array).",
  "$defs": {
    "AudienceConfStrict": {
      "additionalProperties": false,
      "properties": {
        "lookalike": {
          "anyOf": [
            {
              "$ref": "#/$defs/LookalikeStrict"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "partitioning": {
          "anyOf": [
            {
              "$ref": "#/$defs/PartitioningStrict"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "question_targeting": {
          "anyOf": [
            {
              "$ref": "#/$defs/QuestionTargetingStrict"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "subtype": {
          "title": "Subtype",
          "type": "string"
        }
      },
      "required": [
        "name",
        "subtype"
      ],
      "title": "AudienceConfStrict",
      "type": "object"
    },
    "LookalikeSpecStrict": {
      "additionalProperties": false,
      "properties": {
        "country": {
          "title": "Country",
          "type": "string"
        },
        "ratio": {
          "title": "Ratio",
          "type": "number"
        },
        "starting_ratio": {
          "title": "Starting Ratio",
          "type": "number"
        }
      },
      "required": [
        "country",
        "ratio",
        "starting_ratio"
      ],
      "title": "LookalikeSpecStrict",
      "type": "object"
    },
    "LookalikeStrict": {
      "additionalProperties": false,
      "properties": {
        "spec": {
          "$ref": "#/$defs/LookalikeSpecStrict"
        },
        "target": {
          "title": "Target",
          "type": "integer"
        }
      },
      "required": [
        "target",
        "spec"
      ],
      "title": "LookalikeStrict",
      "type": "object"
    },
    "PartitioningStrict": {
      "additionalProperties": false,
      "description": "Strict, but its own before-validator usually reports the typo first.\n\n`Partitioning.validate_scenario` runs at `mode=\"before\"`, i.e. ahead of\npydantic's extra-key check, and rejects any combination of set fields that\nis not one of three known scenarios. An unknown key with a non-None value\ntherefore fails as \"invalid partitioning config, the following fields were\nall set: {...}\" rather than \"extra inputs are not permitted\" -- a different\nmessage, but one that still names the offending key, which is the property\nthat matters. A key whose value is null falls through to the extra check\nand gets the ordinary message.",
      "properties": {
        "max_days": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Max Days"
        },
        "max_users": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Max Users"
        },
        "min_days": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Min Days"
        },
        "min_users": {
          "title": "Min Users",
          "type": "integer"
        }
      },
      "required": [
        "min_users"
      ],
      "title": "PartitioningStrict",
      "type": "object"
    },
    "QuestionTargetingStrict": {
      "additionalProperties": false,
      "description": "Strict, and still self-recursive.\n\n`vars` holds either a leaf `TargetVar` or another `QuestionTargeting`, to\narbitrary depth, so the strict twin has to re-declare the field in terms of\n*itself* -- otherwise strictness stops one level down, which is precisely\nwhere a hand-written targeting tree gets long enough to typo.",
      "properties": {
        "op": {
          "title": "Op",
          "type": "string"
        },
        "vars": {
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/TargetVarStrict"
              },
              {
                "$ref": "#/$defs/QuestionTargetingStrict"
              }
            ]
          },
          "title": "Vars",
          "type": "array"
        }
      },
      "required": [
        "op",
        "vars"
      ],
      "title": "QuestionTargetingStrict",
      "type": "object"
    },
    "TargetVarStrict": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "title": "Type",
          "type": "string"
        },
        "value": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "integer"
            },
            {
              "type": "number"
            }
          ],
          "title": "Value"
        }
      },
      "required": [
        "type",
        "value"
      ],
      "title": "TargetVarStrict",
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Custom audiences derived from responses, and lookalikes of them.",
  "items": {
    "$ref": "#/$defs/AudienceConfStrict"
  },
  "title": "audiences conf",
  "type": "array"
}
