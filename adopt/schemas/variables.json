{
  "$comment": "Generated from adopt/adopt/study_conf.py and adopt/adopt/study_conf_strict.py by adopt/adopt/schema_export.py. Do not edit by hand; run `make schemas`. Body of POST /{org_id}/studies/{slug}/confs/variables (a JSON array).",
  "$defs": {
    "LevelStrict": {
      "additionalProperties": false,
      "properties": {
        "facebook_targeting": {
          "title": "Facebook Targeting",
          "type": "object"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "quota": {
          "title": "Quota",
          "type": "number"
        },
        "template_adset": {
          "title": "Template Adset",
          "type": "string"
        },
        "template_campaign": {
          "title": "Template Campaign",
          "type": "string"
        }
      },
      "required": [
        "name",
        "template_campaign",
        "template_adset",
        "facebook_targeting",
        "quota"
      ],
      "title": "LevelStrict",
      "type": "object"
    },
    "VariableConfStrict": {
      "additionalProperties": false,
      "description": "Strict via a sibling, like everything else, rather than in place.\n\n`planning/conf-extra-fields.md` §2 notes that `VariableConf` is the one\nmodel with no load path -- it is not a field of `StudyConf` and is read\nback only as raw JSON -- so it could take `extra=\"forbid\"` directly, with\nno sibling needed. It gets one anyway, for two reasons. The licence rests\non a fact about today (\"nothing reconstructs a VariableConf from storage\"),\nnot on a property of the model, and a `POST /studies/{slug}/validate`\nendpoint that re-reads stored confs is being built in parallel with this\nchange. And a single uniform mechanism means nobody has to remember which\none model is the exception, for a cost of four lines.",
      "properties": {
        "levels": {
          "items": {
            "$ref": "#/$defs/LevelStrict"
          },
          "title": "Levels",
          "type": "array"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "properties": {
          "items": {
            "type": "string"
          },
          "title": "Properties",
          "type": "array"
        }
      },
      "required": [
        "name",
        "properties",
        "levels"
      ],
      "title": "VariableConfStrict",
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Experimental variables and their levels, used to derive strata.",
  "items": {
    "$ref": "#/$defs/VariableConfStrict"
  },
  "title": "variables conf",
  "type": "array"
}
